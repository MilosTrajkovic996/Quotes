import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            Fazi demo project
        </header>
    );
}

export default Header;
